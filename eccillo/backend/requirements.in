# Direct dependencies only — run `pip install -r requirements.in && pip freeze > requirements.txt` to re-pin
Django>=5.1
djangorestframework>=3.15
django-cors-headers>=4.4
psycopg[binary]>=3.2
python-dotenv>=1.0

# --- Agent layer (Phase 2 AI orchestration) ---
# Multi-provider LLM support + typed schemas.
#   openai     — OpenAI + any OpenAI-compatible server (vLLM, LM Studio, OpenRouter…)
#   anthropic  — Claude
#   gemini     — Google Gemini (via REST, uses httpx; no extra SDK)
#   ollama     — local models (via REST, uses httpx; no key, no extra SDK)
openai>=2.0
anthropic>=0.40
pydantic>=2.9
httpx>=0.27

# --- Async agent turns (Phase 3) ---
celery>=5.4
redis>=5.0
